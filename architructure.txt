Video Frame
     |
     |
     ↓
YOLO Vehicle Detection
     |
     ↓
ByteTrack
     |
     ↓
Vehicle IDs
     |
     |
     ├───────────────┐
     ↓               ↓
Vehicle Box     YOLO Plate Detection
                         |
                         ↓
                 Plate Bounding Box
                         |
                         ↓
                  IoU Matching
                         |
                         ↓
              Vehicle ID + Plate Number
                         |
                         ↓
                    Database

So the matching is basically geometry + tracking, not AI understanding.


######################################################################

  IoU Matching

Video Frame (Frame 100)
          |
          |
   ┌──────┴──────┐
   ↓             ↓

YOLO Vehicle   YOLO Plate
Detector       Detector

   ↓             ↓

Car Box        Plate Box

[100,100,600,500]   [300,400,450,450]

          |
          ↓

   is_inside(vehicle_box, plate_box)

          |
          ↓

Plate belongs to Car

###########################################
ANPR_Project/
│
├── app/
│   │
│   ├── main.py                    # Application entry point
│   │
│   ├── config/                    # Configuration management
│   │   ├── settings.py
│   │   └── config.yaml
│   │
│   ├── core/                      # Core utilities
│   │   ├── logger.py
│   │   ├── exceptions.py
│   │   └── constants.py
│   │
│   │
│   ├── detection/                 # YOLO models
│   │   ├── vehicle_detector.py
│   │   └── plate_detector.py
│   │
│   │
│   ├── tracking/                  # Object tracking
│   │   ├── tracker.py
│   │   └── vehicle_tracker.py
│   │
│   │
│   ├── recognition/               # OCR
│   │   ├── ocr_engine.py
│   │   └── plate_reader.py
│   │
│   │
│   ├── matching/                  # Vehicle-plate association
│   │   ├── iou_matcher.py
│   │   └── center_matcher.py
│   │
│   │
│   ├── database/
│   │   ├── database.py
│   │   ├── models.py
│   │   └── repository.py
│   │
│   │
│   ├── storage/
│   │   ├── image_storage.py
│   │   └── video_storage.py
│   │
│   │
│   └── pipeline/
│       └── anpr_pipeline.py       # Connects everything
│
│
├── models/
│   ├── yolo11_vehicle.pt
│   └── license_plate_detector.pt
│
│
├── data/
│   ├── input_videos/
│   ├── output_videos/
│   └── plate_crops/
│
│
├── logs/
│   └── anpr.log
│
│
├── database/
│   └── anpr.db
│
│
├── tests/
│   ├── test_detector.py
│   ├── test_ocr.py
│   └── test_matcher.py
│
│
├── requirements.txt
├── .env
├── README.md
└── .gitignore


I suggest this order:
1. Implement ANPRPipeline
   Move the processing flow from test_video.py into a reusable class so python -m app.main runs the application.
2. Save recognized plates to SQLite
   Store the video/session ID, vehicle tracking ID, plate text, confidence, and timestamp. Avoid inserting the same vehicle on every frame.
3. Save plate crops and annotated video
   Use the existing output settings in config.yaml.
4. Verify with a short video
   Check tracking consistency, plate-to-vehicle matching, OCR accuracy, and saved records.
Start with ANPRPipeline, then add database